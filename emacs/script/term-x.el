(when (> (length (getenv "DISPLAY")) 0)
  (when (window-system)
    (require 'xclip)
    (turn-on-xclip)
    (require 'xterm-title)
    (xterm-title-mode t)
    (xterm-mouse-mode t))
  (when (not (window-system))
    (setq x-select-enable-clipboard t))
  )
